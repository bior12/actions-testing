{
  "name": "root",
  "workspaces": [
    "actions/*"
  ],
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "lerna run build"
  },
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@kkt/ncc": "^1.1.2"
  },
  "devDependencies": {
    "lerna": "^8.2.2"
  }
}
